var expect = require('expect.js');

describe ('Transmitter', function() {
  describe ('#transmit()', function() {
    it ('should transmit information via UDP', function () {
      expect().fail();
    });

    it ('should transmit a Pulsar packet when given some valid JSON', function () {
      expect().fail();
    });

    it ('should transmit packets to port 6660', function () {
      expect().fail();
    });

  })
})
